***to install dependencies**

pip install -r requirements.txt

**in new terminal**

rasa run action

**in new terminal**

docker run -p 8000:8000 rasa/duckling

**in new terminal**

rasa train
rasa shell
